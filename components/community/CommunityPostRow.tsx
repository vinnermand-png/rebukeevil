import type { CommunityPost, Profile } from '@/lib/community-types';
export default function CommunityPostRow({ post, profile, replyCount, prayerCount }: { post: CommunityPost; profile?: Profile; replyCount: number; prayerCount: number }) { return <a href={`/community/post/${post.id}`} className="group block border-b border-white/[.14] py-7 transition-colors hover:bg-white/[.02]"><div className="flex items-start justify-between gap-5"><p className="mono text-[10px] text-[#85847d]">{post.type} / {post.id.slice(0, 4)}</p><span className="text-xl text-[#aaa9a1] transition-transform duration-300 group-hover:translate-x-1">→</span></div><h2 className="display mt-5 text-[clamp(1.8rem,3vw,3rem)] leading-none transition-transform duration-300 group-hover:translate-x-1">{post.title}</h2><p className="serif mt-3 line-clamp-2 max-w-2xl text-[17px] leading-relaxed text-[#aaa9a1]">{post.body}</p><div className="mt-6 flex flex-wrap gap-x-6 gap-y-2 mono text-[9px] text-[#85847d]"><span>@{profile?.username || 'UNKNOWN'}</span><span>{prayerCount} PRAYED</span><span>{replyCount} REPLIES</span></div></a>; }
