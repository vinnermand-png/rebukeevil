'use client';
import { useState } from 'react';
import { createClient } from '@/lib/supabase/client';
export default function CommunityPostEditor({ userId, onDone, onCancel }: { userId: string; onDone: () => Promise<void>; onCancel: () => void }) { const [type, setType] = useState('PRAYER'); const [title, setTitle] = useState(''); const [body, setBody] = useState(''); const [error, setError] = useState(''); const [saving, setSaving] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); if (!title.trim() || !body.trim()) { setError('TITLE and BODY are required.'); return; } setSaving(true); const client = createClient(); const { error: saveError } = await client!.from('community_posts').insert({ user_id: userId, type, title: title.trim(), body: body.trim() }); if (saveError) { setError(saveError.message); setSaving(false); return; } await onDone(); } return <form onSubmit={submit} className="border border-[#343532] p-5 md:p-7"><div className="mb-8 flex items-center justify-between"><h2 className="display text-3xl">NEW POST</h2><span className="mono text-[9px] text-[#85847d]">THE GARRISON</span></div><label className="mono block text-[10px]">TYPE<select value={type} onChange={event => setType(event.target.value)} className="mt-2 h-12 w-full border border-[#343532] bg-[#050505] px-3 font-sans text-base tracking-normal text-[#dedbd0] outline-none"><option>PRAYER</option><option>ACCOUNTABILITY</option><option>TESTIMONY</option></select></label><label className="mono mt-6 block text-[10px]">TITLE<textarea maxLength={120} rows={2} value={title} onChange={event => setTitle(event.target.value)} className="mt-2 w-full resize-none border border-[#343532] bg-[#050505] p-3 font-sans text-base tracking-normal text-[#dedbd0] outline-none"/></label><label className="mono mt-6 block text-[10px]">BODY<textarea maxLength={3000} rows={7} value={body} onChange={event => setBody(event.target.value)} className="mt-2 w-full resize-y border border-[#343532] bg-[#050505] p-3 font-sans text-base tracking-normal text-[#dedbd0] outline-none"/></label>{error && <p className="mt-5 text-sm text-[#a87873]">{error}</p>}<div className="mt-7 flex gap-5"><button disabled={saving} className="mono border border-[#aaa9a1] px-6 py-4 text-[10px] disabled:opacity-50">{saving ? 'POSTING...' : 'POST'}</button><button type="button" onClick={onCancel} className="mono px-2 py-4 text-[10px] text-[#85847d]">CANCEL</button></div></form>; }
