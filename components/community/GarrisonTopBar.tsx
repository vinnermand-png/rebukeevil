'use client';
import HelmetLogo from '@/components/HelmetLogo';
import { createClient } from '@/lib/supabase/client';
import type { Profile } from '@/lib/community-types';
export default function GarrisonTopBar({ profile }: { profile?: Profile | null }) { return <header className="relative flex min-h-[68px] items-center justify-between border-b border-[#66645d]/40 bg-[#171714] px-5 text-[#f0e8d8] md:px-8"><HelmetLogo/><div className="absolute left-1/2 hidden -translate-x-1/2 items-center gap-5 lg:flex"><span className="h-1.5 w-1.5 bg-[#8c1f1f]"/><span className="mono text-[10px] tracking-[.2em]">THE GARRISON</span><span className="h-1.5 w-1.5 bg-[#a98c50]"/></div><div className="flex items-center gap-4"><span className="hidden mono text-[9px] text-[#a98c50] sm:inline">NETWORK / LIVE</span><span className="h-2 w-2 rounded-full bg-[#a98c50]"/><a href={profile ? `/community/profile/${profile.username}` : '/community'} className="mono text-[10px] text-[#cfc0a2]">{profile ? `@${profile.username}` : 'FIELD MEMBER'}</a><button onClick={() => createClient()?.auth.signOut()} className="hidden border-l border-[#66645d]/50 pl-4 mono text-[9px] text-[#88857b] hover:text-[#f0e8d8] sm:block">EXIT</button></div></header>; }
