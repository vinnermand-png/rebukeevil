'use client';
import { useState } from 'react';
import { createClient } from '@/lib/supabase/client';
export default function AdminLogin({ onLogin }: { onLogin: () => void | Promise<void> }) { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); async function login(e: React.FormEvent) { e.preventDefault(); setError(''); const client = createClient(); if (!client) { setError('Supabase environment is not configured.'); return; } const result = await client.auth.signInWithPassword({ email, password }); if (result.error) setError(result.error.message); else await onLogin(); } return <form onSubmit={login} className="mx-auto w-full max-w-md border border-[#343532] p-6"><h1 className="display text-4xl">REBUKEEVIL ADMIN</h1><label className="mono mt-8 block text-[10px]">EMAIL<input required type="email" value={email} onChange={e => setEmail(e.target.value)} className="mt-2 w-full border border-[#343532] bg-transparent p-3 font-sans normal-case tracking-normal outline-none"/></label><label className="mono mt-5 block text-[10px]">PASSWORD<input required type="password" value={password} onChange={e => setPassword(e.target.value)} className="mt-2 w-full border border-[#343532] bg-transparent p-3 font-sans normal-case tracking-normal outline-none"/></label>{error && <p className="mt-4 text-sm text-[#a87873]">{error}</p>}<button className="mono mt-7 w-full border border-[#aaa9a1] px-4 py-4 text-[10px] hover:bg-[#171714]">SIGN IN</button></form>; }
